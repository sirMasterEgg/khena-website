/** Bentuk minimal kategori & koleksi yang dikirim API publik (contract.md Bagian 32 & 34). */
export type CatalogCategory = {id: string; slug: string; name: string};
export type CatalogCollection = {id: string; slug: string; name: string};
